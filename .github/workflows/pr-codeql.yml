name: "Code scanning"

on:
  pull_request:
    types: [opened, reopened]
  push:
    branches:
      - main
      - development
   
jobs:
  Scan:
    uses: Astridstar/github_workflows/.github/workflows/codeql.yml@main
